# Running benchmarks

Run the harness runtime through an SDK client (the TypeScript client at `packages/sdk/client`, or the .NET client SDK in the dotnet port) and drive the `jsonrpc-agent` minimal variant. Use separate workspaces and session IDs for independent benchmark tasks.

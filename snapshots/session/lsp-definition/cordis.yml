# Exercise the model-facing LSP tool through the shipped Loader entry path.
# The scenario workspace supplies the deterministic stdio server used by this test composition.
- insert:
    - id: lsp
      name: '@deepseek-ai/hsh-lsp'
    - id: lsp-stdio
      name: '@deepseek-ai/hsh-lsp-stdio'
      config:
        servers:
          fixture:
            command: !!js process.execPath
            args: ['./lsp-server.mjs']
            extensionToLanguage:
              '.ts': typescript
    - id: tool-lsp
      name: '@deepseek-ai/hsh-tool-lsp'
      config:
        maxLocations: 1

- id: timeout-policy
  name: '@deepseek-ai/hsh-tool-call-timeout-policy'

feat(dsh): lsp tool parity without node (Phase 7)

The lsp seam completes with the routing service (ctx.lsp), the mounted lsp
tool, and the corpus provider path: node is not used in the ported
version, so the recorded lsp-server.mjs fixture runs as an embedded C#
FixtureLspProvider mounted by the spine lsp row from
DSH_SNAPSHOT_LSP_CONFIG (the runner maps the scenario's lsp-stdio block
and emits kind fixture). The general StdioLspProvider remains for real
language servers. Corpus: 51 passed, 13 drifted, 9 skipped, 0 errored of
73; 53/53 test projects green.

feat(dsh): ralph tool parity (Phase 7)

The fixed Ralph orchestration ports natively: one fresh one-shot child per
round (verbatim fresh-worker prompt, compact previous handoff), the
structured_output child tool (renders the recorded line), the durable
subagent/descriptor event for named-provider children (pre-step listener
gated on AgentOptions.SubagentProvider), report validation, and the
terminal envelope with LF two-space JSON. Corpus: 50 passed, 14 drifted,
9 skipped, 0 errored of 73; 53/53 test projects green.

@inject Harness.Web.App.Slots.SlotRegistry Registry

@foreach (var contribution in Registry.Get(Slot))
{
    @contribution.Factory()
}

@code {
    /// <summary>The slot to render.</summary>
    [Parameter]
    public string Slot { get; set; } = "";
}

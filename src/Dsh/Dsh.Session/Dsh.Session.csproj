<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Dsh.Session</RootNamespace>
  </PropertyGroup>

  <!-- Pure-to-pure edge only: the session event vocabulary carries Dsh.Llm message/chunk types.
       No Cordis.Core reference yet (it has not landed; that is part 2). -->
  <ItemGroup>
    <ProjectReference Include="..\Dsh.Llm\Dsh.Llm.csproj" />
  </ItemGroup>

</Project>

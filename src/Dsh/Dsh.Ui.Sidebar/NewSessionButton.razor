@inject WebLocale Locale
@inject ShellBus Bus

<button class="dsh-sidebar-action" @onclick="OnClick">@Locale.T("newSessionAction")</button>

@code {
    private void OnClick() => Bus.RequestNewSession();
}

# DeepSeek Harness → C# / .NET 10 Conversion Plan

English | [中文](README.zh.md)

This folder holds the plan to convert the repository from a TypeScript monorepo into a **C# / .NET 10** codebase: a **TUI** for the CLI and **Blazor** for the Web UI.

## Locked decisions

These were confirmed with the requester before the plan was written:

- **Web UI model:** Blazor Server / Blazor Web App with server interactivity.
- **Scope:** full .NET port — the Python SDK is rewritten (no Python remains), the docs website is folded into the Blazor app, and the native `landlock-run` sandbox stays native (P/Invoke or sidecar).
- **Sequencing:** proof-of-concept spike first, then an incremental strangler migration, retiring the TypeScript tree only after parity.

## Contents

1. [Objective and scope](01-objective-and-scope.md) — what the conversion targets, what is in and out of scope, and a snapshot of the current state.
2. [Target architecture](02-target-architecture.md) — the .NET solution layout and the technology mapping table.
3. [Key decisions and risks](03-decisions-and-risks.md) — the hard problems (Cordis port, plugin loading, config expressions, Blazor slots) plus the risk register and remaining open decisions.
4. [Execution phases](04-execution-phases.md) — Phases 0–8 with deliverables, exit criteria, and an effort estimate.

## How to read

Read `01` for scope, `02` for the target shape, `03` for the decisions that carry the most risk, and `04` for the order of work. Phase 0 (the Cordis-core + vertical-slice spike) is the go/no-go gate for the whole effort; everything else is sequenced off its outcome.

## Status

**Phase 0 (spike): COMPLETE — 2026-08-30** — integrated on branch `port/dotnet10` (merges `c4b3adc41b`; solution commit included):

- `src/Cordis/Cordis.Core` — Cordis port (Context/Service/Events with all five dispatch modes, Fiber effects) — 37 xUnit tests.
- `src/Cordis/Cordis.Cosmokit` + `src/Cordis/Cordis.Schemastery` — utility + schema-validation ports — 37 console tests.
- `src/Dsh/Dsh.Llm`, `Dsh.Session`, `Dsh.Tools`, `Dsh.Spike` — the vertical slice — 28 console tests, including the byte-pinned 22-event headless smoke.
- Solution `deepseek-harness.slnx`: 10 projects, builds with 0 warnings / 0 errors on .NET 10; the smoke exe prints the pinned 22-event log and exits `== PASS ==`.

Go/no-go verdict: **GO** — the Cordis semantics (waterfall short-circuit, next()-return propagation, reverse-order effect unwind, dispatch modes) port faithfully to C#. Phase 1 (Loader / Include / config expressions) can proceed.

**Phase 1 (Foundations): COMPLETE — 2026-08-30** — integrated on branch `port/dotnet10` (head `ea2b8ce485`):

- `src/Cordis/Cordis.Plugin.Loader` — row/tree model, dependency-gated activation, transactional reconciliation — 10 tests.
- `src/Cordis/Cordis.Plugin.Include` — file-backed entry lists, patch layers with insert-then-patch semantics, the restricted `!!js` expression language (literals, `$env` lookups, if/else, and/or/not, concat, lists, indexing; loud failure otherwise; Roslyn scripting deferred), and a minimal YAML-subset parser — 23 tests.
- `src/Cordis/Cordis.Plugin.Timer` + `src/Cordis/Cordis.Logger.Console` — 28 tests.
- `src/Cordis/Cordis.Plugin.Hmr` — exact-config FileSystemWatcher with coalescing, serialized refreshes, contained failures — 5 tests.
- Pipeline: `Directory.Build.props` (analyzers), C# `.editorconfig`, `.github/workflows/dotnet-ci.yml`, `PIPELINE-README.md`.
- Solution `deepseek-harness.slnx`: 19 projects; full build 0 errors; all seven suites green (168 tests, 0 failures); the Phase 0 headless smoke still exits `== PASS ==`. The cordis.yml-composed boot, patch layers, and reload are covered by the Include and Hmr suites against the real Loader.

Go/no-go verdict for Phase 2 (core spine: session, system-prompt, tools, agent, agent-loop): **GO**.

**Phase 2 (Core spine): COMPLETE — 2026-08-30** — integrated on branch `port/dotnet10` (head `1abcae841b`):

- `src/Dsh/Dsh.Agent` + `src/Dsh/Dsh.Scope` — live agent registry/inbox/status, agent-scoped registrations — 23 tests.
- `src/Dsh/Dsh.Session.Persistence` (JSONL, sync/batched flush), `src/Dsh/Dsh.Session.Projection`, `src/Dsh/Dsh.Session.Titles` — 16 tests, pinned-log replay included.
- `src/Dsh/Dsh.SystemPrompt` — ordered sections, tool-schema providers, tool ordering — 22 tests.
- `src/Dsh/Dsh.Llm.DeepSeek` — real provider: request builder, SSE translation, error mapping; additive `LlmError(message, code, status)` — 49 tests against a fake handler (no network).
- `src/Dsh/Dsh.AgentLoop` — the real turn driver: `turn/start` → inbox claim → `agent/pre-step` (rejectable waterfall) → `step/start` → durable history derivation → `agent/request` waterfall → stream chunks/assistant message → tool-call scheduler → `step/end` → loop → `agent/turn-stopping` → `turn/end`; create/resume factory on the agent registry, request-reconstruction invariant on `llm/stream`, runtime-context snapshot projection, and the session event-type registry so plugin-merged events round-trip the JSONL — 5 console suites (full mock turn with persistence replay, pre-step reject, cancellation, resume + invariant, runtime context).
- Solution `deepseek-harness.slnx`: 23 projects; full build 0 errors; all twelve suites green (283 tests, 0 failures). The `Dsh.Spike` exe now runs both smokes: the pinned Phase 0 scenario (`== PASS ==`) and the Phase 2 gate (`== PHASE2 PASS ==`) — one headless task through the real agent loop against the mock provider (tool call, then text) with the 22-event JSONL log replaying identically.

Go/no-go verdict for Phase 3 (CLI/TUI, boot, and launcher): **GO**.

**Phase 3 (CLI + TUI): COMPLETE — 2026-08-30** — integrated on branch `port/dotnet10` (head `75c1e07a3b` + integration):

- `src/Dsh/Dsh.Cli` — the `dsh` launcher: hand-rolled commander port (launcher-flags-first parsing, verbatim inner-argument pass-through, `web` alias, `plugin` subcommand, `-V`, exact TS error strings/exit codes); profile boot composing `cordis.yml` patch layers through the Cordis Loader/Include ports (in-box `@deepseek-ai/dsh-base`/`dsh-headless`/`dsh-tui` bundles, `profile.json` manifest — documented deviation from package.json, `$DSH_HOME/cordis.patch.yml` home layer, `--patch` overlays); boot-free `--dump-config` with per-layer comments; `dsh plugin add/remove/list` manifest editing (documented deviation from pnpm forwarding until NuGet packaging); the spine row registry (sessions/llm/tools/systemPrompt/agents/agentLoop/sessionPersistence/deepseek/mock/todo/headless/tui; unknown rows fail loud with the row id) — 17 tests.
- `src/Dsh/Dsh.Tui` — Terminal.Gui 1.17.1 interactive UI: live transcript pane, input line driving the real agent loop, todo panel from `todo/write`, tool-call disclosure, and approval dialogs on the `tools/pre-execute` waterfall; fake-driver headless smoke — 4 tests.
- Additive upstream fixes: `YamlSubset` now honors the documented top-level empty flow collections and the `insert:`/nested-block key form.
- Solution `deepseek-harness.slnx`: 27 projects; full build 0 errors; all fourteen suites green (304 tests, 0 failures). Verified smokes on the merged tree: `dsh --profile headless "task"` runs a real loop turn and prints the assistant text; `dsh --profile headless --dump-config` prints the composed layers; `dsh plugin --profile headless list` prints the template bundles; `dsh --profile tui --smoke` boots the interactive UI and exits 0.

Go/no-go verdict for Phase 4 (capability seams): **GO**.

**Phase 4 wave 1 (capability seams): COMPLETE — 2026-08-30** — integrated on branch `port/dotnet10` (head `70e8780687` + integration commits):

- `src/Dsh/Dsh.Todo` — the todo seam promoted from the Phase 0 spike: `ITodoService` (whole-list last-write-wins), `TodoService` provider, `todo_write` consumer, durable `todo/write` event — 6 tests (Spike/Tui/Cli rewired to the seam).
- `src/Dsh/Dsh.Fs` — Service Definition + `LocalFileSystemProvider` (workspace-root containment, staged atomic writes, version tokens) + `fs_read`/`fs_write` consumers — 26 tests.
- `src/Dsh/Dsh.Subprocess` — fully-specified spawn requests, bounded collect with tail+spill, tree-scoped termination, `DSH_*` env scrub — 7 tests.
- `src/Dsh/Dsh.Shell` — the request/spec split, fused timeout/abort first-cause classification, `bash` consumer with the TS render markers — 9 tests.
- `src/Dsh/Dsh.Web` — `WebRuntime` provider selection (full `WEB_PROVIDER_*` codes), `HttpWebProvider` (fake-handler tested, zero network), `web_fetch`/`web_search` consumers — 51 tests.
- `src/Dsh/Dsh.Credentials` — credential-reference definition, `.env`-layered local provider (env > managed > project > user), redaction that never leaks values — 40 tests.
- `src/Dsh/Dsh.Skill` — registry + filesystem provider (SKILL.md discovery) + catalog consumer — 8 tests.
- `src/Dsh/Dsh.Settings` — typed sections, installSection read-through semantics, JSON file provider, secret redaction — 10 tests.
- `src/Dsh/Dsh.Identity` — stable anonymous user id under `$DSH_HOME`, corruption fails loud — 6 tests.
- `src/Dsh/Dsh.Plan` — plan/write logged state, `plan_write` consumer, JSONL round-trip — 7 tests.
- All ten seams are mounted in the CLI spine (`dsh-base` bundle rows + `SpineRegistry`), so a headless boot composes the full wave-1 spine.
- Solution `deepseek-harness.slnx`: 39 projects; full build 0 errors; **all twenty-four suites green (486 tests, 0 failures)**; the headless one-shot and the TUI smoke both still exit 0 with the full spine mounted.

Go/no-go verdict for Phase 4 wave 2 (terminal, lsp, context, compaction, subagent, jobs, workflow, webhook, preset, guard, hooks, session-query, storage, workspace, attachment, spill, goal, schedule, feedback, sandbox+native bridge): **GO**.

**Phase 4 wave 2 (capability seams): COMPLETE — 2026-08-30** — integrated on branch `port/dotnet10` (head `98eccb5087` + integration):

- `src/Dsh/Dsh.Goal` (goal/write logged state + goal_write tool, 12), `src/Dsh/Dsh.Schedule` (timer-backed once/recurring tasks, 8), `src/Dsh/Dsh.Feedback` (message feedback logged state + tool, 8) — 28 tests.
- `src/Dsh/Dsh.Storage` (JSON file store, revisions, 12), `src/Dsh/Dsh.Workspace` (workspace identity/lifecycle, 9), `src/Dsh/Dsh.Spill` (spill file registry + age sweep, 12), `src/Dsh/Dsh.Attachment` (file ingest/list/read/remove, 10) — 43 tests.
- `src/Dsh/Dsh.Compaction` (budgeted head/tail trim, turn-aligned cuts, 8), `src/Dsh/Dsh.Context` (contributor assembly: agent instructions, file/session references, time, 7), `src/Dsh/Dsh.SessionQuery` (log filters, turns, folds, 8) — 23 tests.
- `src/Dsh/Dsh.Preset` (cordis.yml preset discovery/composition + persona, 14), `src/Dsh/Dsh.Guard` (repeat-tool reminder at the TS thresholds + tool-timeout policy, 20) — 34 tests.
- `src/Dsh/Dsh.Jobs` (in-process job registry: ids, consuming reads, kill, bounded teardown + job_output/job_list/job_kill tools, 20), `src/Dsh/Dsh.Workflow` (host-registered step definitions on worker tasks, lifecycle events, workflow tool with durable run records, 18) — 38 tests.
- `src/Dsh/Dsh.Terminal` (persistent sessions, bounded scrollback, consuming sends — redirected stdio, ConPTY deferred, 5), `src/Dsh/Dsh.Hooks` (hook-protocol: matcher, outcome codec, log-only hook/invoked + hook/result events, 10), `src/Dsh/Dsh.Lsp` (Content-Length JSON-RPC framing + client, 4), `src/Dsh/Dsh.Subagent` (in-process delegation: delegate/cancel/teardown, 5) — 24 tests.
- All sixteen new seams are mounted in the CLI spine (dsh-base bundle rows + `SpineRegistry`), so a headless boot composes the complete wave-1 + wave-2 spine.
- Solution `deepseek-harness.slnx`: 61 projects; full build 0 errors; **all forty-two suites green (676 tests, 0 failures)**; the headless one-shot and the TUI smoke both still exit 0 with the full spine mounted.
- Deferred to wave 3 (documented in the seam sources): sandbox + native landlock bridge, webhook ingress, out-of-process subagent drivers, LSP process host + tool, PTY/ConPTY terminal backends, and the per-seam deferred surfaces each port names.

**Phase 4 wave 3 (capability seams): COMPLETE — 2026-08-31** — integrated on branch `port/dotnet10` (merges of `port/dotnet10-p4-auth` and `port/dotnet10-p4-sandbox` + wave-3 commits):

- `src/Dsh/Dsh.Authorization` — the credentials authorization half: one attempt per key, decline/cancel semantics, commit-confirmed success through a write-observing credentials facade (the `credentials/record-updated` event is not ported yet), `authorization/settled` fan-out — 23 tests.
- `src/Dsh/Dsh.Sandbox` — the seam core (escalation vocabulary, writable-roots derivation, mode-`none` provider) plus the confine contract and the Landlock sidecar backend: `--probe` enforcement probing (full/partial), argv wrapping per the native `landlock-run` contract, and fail-closed refusal (`SANDBOX_UNAVAILABLE`) when no usable sidecar exists; the native binary stays in `native/` until cutover — 19 tests.
- `src/Dsh/Dsh.Webhook` — rule registry with contained dispatch and abort-and-drain teardown, the GitHub HMAC-SHA256 handler (exact status/message vocabulary, bounded body, strict UTF-8), and a loopback `HttpListener` ingress; the session-creation action is a required composition hook deferred with the Phase-5 agent/workspace/preset spine — 25 tests.
- `src/Dsh/Dsh.Subagent` — provider registry over named drivers, the in-process driver as a named provider, and the DSH SDK out-of-process driver: one child runtime per delegation over newline-delimited JSON-RPC (initialize/session/prompt/shutdown), the assistant-output fold, the `sdkChildOutcome` reason mapping, and the idempotent dispose ladder; the child runtime server arrives with the SDK phase, and a scripted fake child pins the wire contract — 20 tests.
- `src/Dsh/Dsh.Lsp` — streaming Content-Length decoder (65536-byte header cap), structural-guard protocol translation, the JSON-RPC connection over a private process handle (the subprocess seam has no pipe modes yet; the spawner seam makes the swap mechanical), the serialized abortable server instance with the transient didOpen/didClose lifecycle, `$/cancelRequest` grace, and the shutdown/terminate ladder, plus the pure tool renderers — 90 tests against a fixture server.
- `src/Dsh/Dsh.Terminal` — the ConPTY backend (Windows): pseudo-console spawn sequence (canonical Microsoft-sample shape), controlled-prompt readiness (`stdin_read`/`inferred_idle`), OSC 133 sanitizer, serialized resize, ClosePseudoConsole → tree-kill teardown. The session suites probe the host first: some Windows hosts (observed on Windows 11 build 26200) fail every console-subsystem child under a pseudo console with `STATUS_DLL_INIT_FAILED` while GUI children attach fine, so those suites skip with the reason on such hosts and run in full on healthy ones; the sanitizer and readiness model are pinned by host-independent tests — 13 tests + 9 host-gated ConPTY suites.
- All wave-3 seams are mounted in the CLI spine (`dsh-base` bundle rows + `SpineRegistry`); the sandbox and terminal rows select their backend by config, and the `dotnet-ci.yml` lane now runs every console suite.
- Solution `deepseek-harness.slnx`: 98 projects; full build 0 errors; **all console suites green**; the headless one-shot and the TUI smoke both still exit 0 with the full spine mounted.
- Still deferred (named in the seam sources): LSP provider pooling/registry + fs host helpers, SDK/ACP subagent child servers, Unix pty, and the per-seam named surfaces (fs diff/edit tools, storage/session-query sqlite, context digests, goal fold, attachment image admission, compaction meter, skill layering, workspace registry ordering, spill retention policy, schedule selectors, observation policy).

**Phase 4 (capability seams): COMPLETE — 2026-08-31** — every seam in the Phase-4 list ships Service Definition + Provider + Consumer with console-suite coverage and a spine mount.

Go/no-go verdict for Phase 5 (Web — Blazor): **GO**.

**Phase 5 wave 1 (web foundation): COMPLETE — 2026-08-31** — integrated on branch `port/dotnet10`:

- `src/Dsh/Dsh.Web.Host` — the gateway and host: the RPC method registry (the Typert local-registry runtime half; effect-owned registrations; the TS failure vocabulary — `gateway/bad-request`, `cancelled`, `internal`, `invocation-unavailable`, `arguments-invalid`, `signature-invalid`), the unary HTTP carrier (`POST /api/<namespace>/<method>` with the client-request/server-response envelopes, rpcId echo, the 404/415/400/413 status vocabulary, the 300 MiB body cap, the segment grammar), the raw WebSocket mux at `/api/remote.mux` (open/cancel vs item/end/error frames, close codes 1003/1008, serialized writes; the TS 2-second ping heartbeat is not reproduced — the managed WebSocket API cannot emit protocol pings, documented), the `$events` forwarded-event stream (ready frame with clientId + host home, then live emits over the port's own event selection), and the Cordis-owned `WebHostService` booting Kestrel with the context in DI — 22 suites.
- `src/Dsh/Dsh.Web.App` — the Blazor shell (server interactivity): the slot registry + outlet (the ui-slots port), the observable web session store over the Cordis event stream (post-commit notifications only), and the chat page (session list, transcript with user/assistant/tool bubbles, live input) driving the ported agent loop with mock/deepseek routes — pre-rendered shell verified over HTTP.
- The shell's chat view gains live-state parity: the web session store now projects running (agent/status), queued (the three agent/inbox events, from the live inbox counts), and the last agent failure (agent/error, cleared when a new activity starts) per session entry, and every product-visible string rides the typed `WebLocale` dictionary through `t()` (the "client UI copy is locale-owned" rule; English is the only shipped locale, missing keys render as themselves). The chat page renders the running/queued state in the session list and an error banner in the transcript — 79 host suites, and the pre-rendered shell smoke now drives one real mock turn over the wire (session/create + session/prompt) and asserts the locale copy, the session row, and the user/assistant transcript text in the served HTML.
- Stream methods now dispatch through the mux (unary-on-stream and stream-on-unary settle `gateway/signature-invalid`), and the session remotes land: the `SessionWireEvent` projection (envelope lifted out of the C# event data), `session/page` (throughSeq/beforeSeq/maxMessages windowing, `session/not-found`), and `session/follow` (snapshot header/cursor/records then gap-checked live event frames over the 0-based C# log); `session/list` and `session/create` align to the wire shapes — 26 host suites, including real mock-LLM turns through the loop spine.
- The `dsh-web` profile bundle (`rpc` + `webHost` rows) makes `dsh web` serve the shell on the configured host/port; verified end-to-end: GET / serves the interactive shell, SignalR and HTTP invokes drive full mock-LLM turns, and the mux forwards live session events.
- The settings, credentials, and workspace remote namespaces land over the ported seams: `settings/describe` (redacted catalog with writable/hasDocument, the schema `toJSON()` refs envelope ported onto `Cordis.Schemastery.Schema`, live/restart applies, secret slots, revisions) and `settings/update`/`settings/replace` (patch merge and wholesale replace, refusals classified `settings/conflict {ns, expected, actual}` vs `settings/rejected {ns}`); `credentials/describe` (batch ≤ 64 refs, grammar `^[A-Za-z_][A-Za-z0-9_]*$`, per-ref configured/source/writable — never the value), `credentials/set`/`unset` (non-empty value gate, shadowed writes refuse `credential/rejected {ref}`); `workspace/create` over the ported single-slot lifecycle (workspace view with id/title/path/sessionIds and ISO-8601 instants, idempotent re-open answers `created: false`, failures classify `workspace/invalid-path {path}`). The namespaces stay registered without a provider and answer an actionable `gateway/internal` (TS controller parity); the `settings` spine row (FileSettingsProvider, `<dshHome>/settings.json` — JSON-only deviation from the TS YAML default) joins the dsh-base bundle, and the open-string `RpcDomainError` lets the gateway transport any code verbatim — 44 host suites.
- `settings/mutate` path ops land (the TS path-op model): ordered `{op:'set'|'unset', path: string[], value?}` edits over the seam's serialized write chain — later ops observe earlier ones, a deep set creates intermediate objects, an unset through an absent path is already satisfied, the empty path addresses the section root, and the motivating case works end to end: a caller holding only the redacted descriptor can name a `role('secret')` field without restating the section (a wholesale replace rebuilt from a redacted document would silently delete it). Wire-level op-shape violations refuse `gateway/bad-request` before the write; seam refusals classify `settings/conflict`/`settings/rejected` as before — 62 host suites, 16 settings suites.
- The loopback process-token fence lands (the §1.6 shape the spec requires "in place"): the Host/Origin trust fence (403 for a rebound Host, a cross-site fetch marker, or a mismatched Origin), the browser-session cookie (401 when absent or invalid), and the launch-token exchange that mints the authority-bound HMAC-signed cookie (303 + Set-Cookie, HttpOnly/SameSite=Strict, 24 h max-age). The fence gates the index, the gateway, the hub, and the mux (upgrades rejected with plain HTTP 401/403) and the Blazor circuit; `dsh web` prints the authenticated URL carrying the launch token at boot. The signing secret is per host instance — the TS persists it in a credential record so cookies survive restarts; here a restart invalidates cookies and the operator reopens the printed URL (documented reduction). The `directoryPicker` stub also lands: every verb answers `directory-picker/unavailable` (no picking backend composed), with the create name grammar still enforced first — 59 host suites.
- The `session/control` stream lands: one baseline frame (per-session queues from the live inbox — next-turn then next-step, items placed `queued` with their content; per-session jobs via `IJobsService.List`; the consistent projection cut via the sessionProjections registry, now mounted in dsh-base) then live deltas — a queue frame per `agent/inbox/inserted|claimed|discarded` event carrying the full current items (idempotent; driven by the port's own inbox events because the C# session vocabulary does not carry the durable `agent/inbox/spliced` event — the Inbox seam documents the deviation) and a jobs frame per `OnJobsChanged` (unowned jobs have no wire seat). Live projection deltas are deferred (no per-key change events). Cancellation now ends every registry stream quietly (the mux no longer answers a cancel with an error frame — the TS cancel contract) — 67 host suites.
- The settings openers land, completing the settings namespace: `SettingsProvider.PrepareDocument` (materialize an absent local document; null for non-file storage), `settings/openSettingsDocument` (prepare then open through the native text opener, with the cancelled/preparation-failed/no-local-document/open-failed vocabulary), `settings/canOpenAgentPresetDirectory`, and `settings/openAgentPresetDirectory` (empty id → `gateway/bad-request`; no preset service → `agent-preset/not-found {agentPreset, available: []}`; missing preset → `agent-preset/not-found` with the discovered ids; no native opener → `{opened: false, path}`; else open the preset's directory). The native openers are an injectable seam (`SettingsOpeners`, shell-open default through the OS desktop handler); the C# preset seam has no trust classification, so `agent-preset/read-only` stays deferred with the shipped-preset concept — 76 host suites.
- The durable workspace registry lands (the port of the TS WorkspaceRegistry core): one storage-backed document (the JSON storage seam, `workspace_registry` store) holding every workspace record, the display order, the archive set, and explicit session membership — loaded at service start, persisted on every committed mutation, and announced through `workspace/upserted|removed|order|archived` events. The command surface covers create (canonical-path validation with the seam codes, duplicate-path rejection), resolveByPath, rename (unique non-blank titles), delete, insertBefore (DOM-like order moves), attach/insertSessionBefore (explicit membership — the TS derives it from session-persistence headers, which the C# port does not carry, documented), and archiveSession (validated against the live session store via the injected predicate). Mounted as a `workspaceRegistry` dsh-base row over the storage row — 19 workspace suites (10 registry), full solution 0 errors.
- The workspace remote commands + follow stream land on the registry, completing the workspace namespace: `workspace/create` (resolveByPath idempotence → `created: false`, registry create otherwise; every seam failure wraps `workspace/invalid-path {path}`), `workspace/rename` (blank title → `gateway/bad-request`, duplicate → `workspace/name-conflict {name}`, missing → `workspace/not-found {workspaceId}`), `workspace/delete` (`{deleted: true}` or not-found), `workspace/insertBefore` (`{workspaceIds}` or not-found, the TS order-error mapping), `workspace/insertSessionBefore` (membership moves; non-members → `workspace/move-invalid {workspaceId, sessionId, beforeSessionId?}`), `workspace/archiveSession` (unknown sessions → `session/not-found {sessionId}`; answers the full archive set), and `workspace/follow` (baseline `{items, archivedSessionIds}` then upsert/remove/order/archived deltas from the registry events). Workspace views carry the real session membership — 85 host suites.
- Live projection deltas land, completing the `session/control` catalog item: the control stream now diffs each session's projection cut after every committed `session/event` (seeded from the same state the baseline reads, so the first delta only carries real changes) and emits one `projection {sessionId, key, value, seq}` frame per changed key — full-value and idempotent, with non-JSON views omitted like the baseline omits them — 86 host suites.
- The fence's signing secret becomes persistent when a credentials seam is composed: the web host resolves (or creates) a `DSH_WEB_SESSION_SECRET` reference in the managed store, so cookies survive host restarts like the TS credential record (an operator-supplied environment value wins; a malformed stored secret fails loud). The per-instance random fallback remains only for hosts without the credentials seam — 87 host suites.
- The Blazor interactivity stack lands with browser evidence: a headless-Chrome smoke against the published `dsh web` CLI opens the launch-token URL, exchanges the fence cookie, watches blazor.web.js boot a live circuit (StartCircuit → attachWebRendererInterop → RenderBatch replacing the prerendered markup), types a chat message, and dispatches change + form-submit through Blazor's delegated listeners — the ported agent loop runs the mock turn, the prompt echoes in the transcript, the assistant answers "Todo list recorded.", and the session row appears in the session list (observed circuit frames: BeginInvokeDotNetFromJS DispatchEventAsync → RenderBatch → EndInvokeDotNetFromJS). The fixes the smoke forced: the hosting CLI is an SDK.Web binary with `RequiresAspNetWebAssets` (the `_framework` assets are Web-SDK-internal), the WebApplication content root is `AppContext.BaseDirectory` (the published wwwroot lives next to the binary, not in the launcher CWD), and the Router/RouteView stay static because their templated/typed parameters cannot cross an interactive boundary — pages opt in with `@rendermode InteractiveServer`. The Razor source generator on SDK 10.0.400 also served stale per-file output for Dsh.Web.App after an interrupted build: `.razor` edits stopped invalidating the incremental cache, so incremental builds kept emitting `@bind`/`@on*` as literal attributes (the page rendered but had no event handlers — typing did nothing and submit navigated natively). Clean builds regenerate the correct EventCallback/delegation code (probes across stock templates, the Sdk.Web-library shape, `@rendermode`, and `@using static` all compile fine), and the component `@using`s now join `_Imports.razor`; razor edits require a clean build on this machine until the SDK is upgraded. 40 console suites green, full solution 0 errors, browser smoke all-pass (the `_content/Dsh.Web.App/dsh.css` re-base to the root stays a cosmetic 404).
- Auth beyond the loopback fence lands: the fence accepts declared `trustedHosts` authorities — the webHost row takes a `trustedHosts` list, every entry is asserted as a bare `host[:port]` authority at host start (a malformed grant fails the boot like the TS plugin load), and `IsTrustedRequest` matches the TS way: a port-less entry trusts the hostname on any port, an explicit entry trusts the exact authority with the http default port dropped on both sides, IPv6 literals bracketed, numeric dotted literals validated as canonical IPv4. The preset seam gains trust classification (`PresetTrust.System`/`User` on every roster row and resolved preset, carried from the provider's root trust; the preset row takes a `trust` config), and the preset directory opener refuses a system preset with `agent-preset/read-only {agentPreset, reason: "it ships with the deployment"}` exactly like the TS — the default spine root stays user-authored, so a deployment mounts a system root explicitly. — 94 host suites, 15 preset suites.
- Locale dictionaries land: the shell copy dictionary is bilingual (English + Simplified Chinese, the repo's bilingual pair) with the fallback chain active → English → key, the locale is negotiated from the browser's Accept-Language (first supported primary subtag; English default), pinned per request by the shell's locale middleware, and carried across the prerender → circuit boundary through PersistentComponentState — the headless-Chrome smoke now runs with `Accept-Language: zh-CN` and proves the Chinese shell prerenders AND survives the interactive attach, then completes the full mock turn. The scoped `WebLocale` is a facade over the per-scope `LocaleScope`, so the page's pin in `OnInitialized` (which runs after DI resolution) always applies to the render that follows. — 99 host suites.
- The Roslyn source generator for typed codecs lands (the typert codec half): `src/Dsh/Dsh.Rpc.Generator` marks a record `[RpcCodec]` and generates a static `<Name>Codec` with property-by-property `Encode`/`TryDecode` (string/int/long/double/bool/JsonElement, nullable forms, nested `[RpcCodec]` records; unsupported members fail the build with `#error`). The gateway now encodes the error vocabulary through the generated `RpcErrorCodec` (the unary carrier's result-error branch and the mux's error frame), so the hand-written JsonElement plumbing is replaced by compile-time-checked wire code; the codec tests pin the exact `{code, message, details}` shape, the round trip, and the refusal vocabulary, and the gateway/mux suites prove the wire is unchanged over the real carriers. — 103 host suites.
- The interaction capability seam lands with the `$events` waterfall settlement (the final deferred wire surface): `src/Dsh/Dsh.Interaction` ports the ask-user/approval surface — the approval service (`approval/request` waterfall, the closed outcome vocabulary allowed-once/rejected/cancelled/unavailable, the ask/never session policy with the `approval/policy` override, the turn-enclosed `approval/asked`+`approval/decided` audit pair registered in the session event registry), the user-questions service (`user-questions/ask` answerer waterfall with the ASK_ABORTED/EMPTY_QUESTIONS/UNAVAILABLE taxonomy), and the `ask_user_question` model-facing tool. The web host now bridges both waterfalls onto live `$events` streams: proposals forward as `{type: "waterfall", event, eventId, agentId, request}` frames with the request projected JSON-safe, an aborted request or closing stream delivers `{type: "cancel", eventId}` and settles the ask cancelled, and the `$events/result` unary settles the pending continuation — `next` delegates to the chain, `result` maps the value into the closed vocabulary, `rejected` restores the remote error and fails the ask closed; an unknown clientId settles `gateway/internal` ("identifies no active event stream"), an unknown eventId on a known client acks as the TS no-op, and a malformed payload settles `gateway/bad-request`. The spine mounts the `approval` (policy config), `userQuestions`, and `toolAskUser` rows, and the webHost row owns the settlement — 111 host suites, 12 interaction suites.
- The ui-* plugin set lands as Razor Class Libraries contributing to the shell's slots: `Dsh.Ui.Sidebar` (brand row + New Session action into the `sidebar` slot), `Dsh.Ui.Sessions` (the session list with running/queued state into the `sessions` slot), `Dsh.Ui.Chat` (the composer into the `chat.composer` slot), and `Dsh.Ui.Workspace` (the workspace list over the registry seam, live from its events, into the `sidebar` slot). The chat page is now slot-composed: it renders the slots and owns only the transcript and the loop turn, with selection through the shared scoped `ShellState` and gestures through the `ShellBus` (new session / send) — the contributions never know the page's internals. The spine webHost row owns the shared `SlotRegistry`; the dsh-web bundle mounts the four ui rows. The remaining TS ui-* packages (settings-*, plan, goal, jobs, skill, subagent, tool, trajectory, approval surfaces, permission presets, agent-preset, conversation/renderer, primitives, theme, brand-official, user-questions, workflow-run, directory-picker, layout, attachment, input-trigger, commands, model-selection, message-feedback, deliverables, reference, schedule) await the surfaces they build — the port's shell has no settings/plan/… pages yet (documented, surface-gated). — 112 host suites, and the headless-Chrome smoke drives the slot-composed shell end to end (composer → bus → loop turn → reply, zh locale intact).
- The follow-up surfaces land, closing the remaining surface-gated deferrals: the in-shell approval UI (`Dsh.Ui.Approval`, the ui-approval port) answers the interaction waterfalls in-process — the approve/deny dialog for `approval/request`, a text dialog for `user-questions/ask`, and the tools/pre-execute adapter that routes every shell tool call through the approval seam while the shell is live (the TUI keeps its own dialog on its own profile; the headless profile stays unapproved). The settings and plan pages land as routed ui-* pages (`/settings` shows the settings document path and the redacted namespace catalog from the settings seam; `/plan` shows the selected session's plan fold from the plan seam, following the shared selection live) with sidebar nav links; the endpoint-level route table registers the ui-* page assemblies at map time (`AddAdditionalAssemblies`), which required the web bundle to order webCore (the registries) before the ui-* rows and webHost (the map) last. The fence gains the LAN-binding trusted-host derivation (the TS `resolveLanTrust`): binding `0.0.0.0` derives the machine's non-loopback IPv4 literals as port-less trusted authorities, with configured entries following. — 113 host suites, and the headless-Chrome smoke now drives the full loop end to end: submit → the approval dialog appears for the mock turn's tool call → Approve → the reply renders.
- The port's standing documented reductions beyond the Phase-5 set stay as named in the seam sources (the TS gui's remaining pages and their ui-* packages — theme switching, tool/trajectory cards, the conversation renderer, directory pickers, etc. — await their surfaces).

**Phase 6 (SDK + ACP + hooks): IN PROGRESS — 2026-08-31** — integrated on branch `port/dotnet10`:

- The JSON-RPC protocol port lands (`src/Dsh/Dsh.Sdk.Protocol`, the port of `packages/sdk/protocol`): the newline-delimited JSON-RPC 2.0 stdio transport (`JsonRpcLineTransport` — request/response correlation with `req_*` ids, error responses as `JsonRpcResponseError` with the wire code/data, `-32601` for a missing handler, `-32603` for a handler failure, notifications dropped without a handler, malformed lines ignored, cancellation that detaches only when the request settles, and closure/EOF failing every pending request) plus the wire contract — the `initialize`/`session/prompt`/`shutdown` method names, the wire-stable server identity, the request/result records, and the four server-to-client notification payloads over the ported `ContentBlock`/`SessionEvent`/`SubagentStopReason` types.
- The SDK runtime server lands (`src/Dsh/Dsh.Sdk.Server`, the port of the TS `HarnessSdkJsonRpcServer`): over one booted harness context and the transport it validates and records the SDK route (`initialize` — a malformed reasoning effort or non-positive token cap fails loud, an unowned `deepseek-official` route mounts the DeepSeek adapter, any other unregistered provider fails), lazily creates the agent+session pair on the ported agent loop and enqueues the user message (`session/prompt`, with the durable message id returned and the route's `maxTokens` flowing into `AgentOptions`), and disposes the server-owned sessions, adapter, and subscriptions to quiescence (`shutdown`). `session.event` and `session.status` stream live over the transport, and the prompt-block codec (durable blocks via the session log's polymorphic codecs, inline images by wire shape) ships with the protocol. The server now projects every session record to the SDK wire envelope (`{type, seq, timeMs, data}` — the TS `SessionEvent` shape both SDK clients and the subagent fold read; the ported records keep their payload inline, so the projection strips the envelope fields and wraps the rest under `data`). Documented reductions: the `subagent.started`/`subagent.finished` notifications await the port's subagent lifecycle events and parent lineage (the session header carries none), inline image prompt blocks are rejected until the attachment seam admits base64, and the reasoning effort validates but has no `AgentOptions` seat (the loop's call config does). — 43 console suites total (12 protocol + 8 server).
- The .NET client SDK lands (`src/Dsh/Dsh.Sdk.Client`, the port of `packages/sdk/client`, replacing the Python SDK): `HarnessClient` spawns the runtime as a subprocess and owns it to quiescence — the launch resolution (`SdkLaunch.ResolveLaunch`, the port of `resolveDshLaunch`: profile, patches, home, process cwd, and env overrides with `DSH_HOME`; a `.dll` entry spawns via `dotnet`; the default entry is the current executable, other hosts name `DshBin` explicitly), the typed `initialize`/`session/prompt` surface with the wire-identity and message-id validation (`SdkProtocolError`), the per-request timeout as an abandonment (`RequestTimeoutError`, the pending entry drops while the server-side work runs to close), notification subscriptions with filter/queue/waiter/failure semantics and the session-tree scope derived from `subagent.started` lineage edges, and the close ladder — a best-effort `shutdown` bounded by `shutdownTimeoutMs`, then stdin EOF with the EOF grace, then the forced tree kill (Windows has no graceful signal, so the ladder skips SIGTERM exactly like the TS does on win32). The high-level `DeepSeekHarness` (start/retry handshake, `session()` handles, `RunAsync` returning the owned `RunResult` interval from the durable enqueue receipt through the idle transition) and the wire helpers (`NormalizeInput`, `ValidatedSessionEvent`, `FinalResponse`) complete the surface. The `sdk` runtime profile lands as the shipped `dsh-sdk` bundle (`sdkRuntime` spine row over console stdio, exiting on stdin EOF — the `SdkJsonRpcServer` booted by `dsh --profile sdk`, which the subagent seam's `sdkSubagent` row already names as its default child profile). The client's enqueue receipt is the `user/message` session event carrying the queued message id — the port's inbox seam logs no `agent/inbox/spliced` event (documented deviation), and plugin event types unknown to the client process pass through under their envelope shape. The transport now dispatches incoming requests concurrently (the TS event-loop behavior): the reader must keep reading while a handler runs, or a handler's own outbound request (the ACP `requestPermission` bridge) and the notifications behind it could never be processed — responses stay id-correlated. — 44 console suites total (12 protocol + 8 server + 11 client).
- The ACP server lands (`src/Dsh/Dsh.Acp`, the port of `@deepseek-ai/dsh-acp`): the standard Agent Client Protocol (version 2025-03-26) over one JSON-RPC transport with the wire-stable agent identity `deepseek-harness-acp`/`0.0.1` — the full method surface (`initialize` with the capability declaration, `authenticate`, `session/new` with workspace validation and lazy agent+session creation on the ported loop, `session/list` with the base64url keyset cursor over the persisted headers, `session/resume` restoring a stored log through the loop's resume flow, `session/close`, `session/setConfigOption`, `session/prompt`, and the `session/cancel` notification), the ordered committed updates (`agent_message_chunk`/`agent_thought_chunk` from assistant messages, `tool_call`/`tool_call_update` from the tool lifecycle, the `sessionUpdate` discriminator carried on every concrete update), and the machine-policy approval channel — the `tools/pre-execute` gate asks the composed answerers (the ApprovalService) and the `approval/request` waterfall routes owned sessions' one-shot decisions to the client as `session/requestPermission`, mapping `allow-once`/`reject-once`/`cancelled` into the closed outcome vocabulary. The `acp` runtime profile ships as the `dsh-acp` bundle (`approval` + `acpRuntime` rows over console stdio, exiting on stdin EOF; the route follows `DEEPSEEK_API_KEY` like the headless/web rows). The session store's `Remove` is exercised by the resume flow exactly as documented. Documented reductions: MCP mounts are refused until the port has an MCP client seam, inline image prompts await the attachment admission seam, the model option advertises the session's fixed route only (the loop reads `AgentOptions` at creation; no catalogs or reasoning efforts), usage updates await the port's token meter, the persisted header carries no origin/parent/cwd so the resume checks are existence-only and the list's workspace filter is vacuous, and prompt cancellation flows through `session/cancel` only (the ported transport has no server-side request abort). — 45 console suites total (13 ACP).
- The Claude Code and Codex hook bridges land, completing the ported hooks seam (`src/Dsh/Dsh.Hooks`, the ports of `@deepseek-ai/dsh-hooks-claude-code` and `@deepseek-ai/dsh-hooks-codex` over the ported hook-protocol core): the shared runner (`HookRunner` — command hooks through the shell seam with the per-hook timeout override, the bridge-owned default timeout, the trusted stdin payload and dialect environment, the trailing-newline framing difference, and the expected-event-name scoping; infrastructure rejection becomes a no-exit-code outcome so a hook never crashes the turn), the restrictive merge (`HookMerge` — deny > ask > allow precedence, sticky first `continue:false`, reasons for the winning rank, ordered context/system-message accumulation), the detached-run quiescence tracker (`DetachedRuns` — the SessionStart runs are tracked, aborted on disposal, and drained), the durable invoked/result pair helpers (`HookLog` with the 500-char stderr summary cap), and the dialect config parsers (Claude's seven-event matcher-group format with `${CLAUDE_PLUGIN_ROOT}`/`${CLAUDE_PROJECT_DIR}` substitution and skipped non-command hooks; Codex's five-event subset with `async`/non-command skips and the `timeout`/`timeoutSec` alias). The Claude bridge listens on `agent/session-start`, `agent/pre-step`, `tools/pre-execute`, `tools/post-execute`, and `agent/turn-stopping`, building the CC payloads (session_id, transcript_path, cwd, hook_event_name, source/prompt/tool fields, stop_hook_active) with `CLAUDE_PROJECT_DIR` exported and matchers in the literal-alternation mode; the Codex bridge owns the snake_case payloads (model, permission_mode, turn_id, `{ command }` tool_input), regex-only matchers, no trailing newline, and the clean-plain-stdout-as-context rule. The shell request/spec gained the extra-env slot the dialect environment flows through. The spine mounts `hooksClaudeCode` and `hooksCodex` rows (deployment-owned `configPath`). Documented reductions: the ported session header carries no workspace cwd (payload cwd and hook workdir fall back to the process cwd), SubagentStart/SubagentStop parse but never fire (the port's subagent seam has no start/end lifecycle events), a hook `ask` maps to a deny (no pre-tool ask seat), the post-tool `additionalContext` injects into the next step (no additional-context slots on the tool decisions), and a blocking Stop hook force-continues unbounded (the TS's loop-guard TODO). — 45 console suites total (23 hooks, incl. the real-process bridge turns).
- The ACP server lands (`src/Dsh/Dsh.Acp`, the port of `@deepseek-ai/dsh-acp`): the standard Agent Client Protocol (version 2025-03-26) over one JSON-RPC transport with the wire-stable agent identity `deepseek-harness-acp`/`0.0.1` — the full method surface (`initialize` with the capability declaration, `authenticate`, `session/new` with workspace validation and lazy agent+session creation on the ported loop, `session/list` with the base64url keyset cursor over the persisted headers, `session/resume` restoring a stored log through the loop's resume flow, `session/close`, `session/setConfigOption`, `session/prompt`, and the `session/cancel` notification), the ordered committed updates (`agent_message_chunk`/`agent_thought_chunk` from assistant messages, `tool_call`/`tool_call_update` from the tool lifecycle, the `sessionUpdate` discriminator carried on every concrete update), and the machine-policy approval channel — the `tools/pre-execute` gate asks the composed answerers (the ApprovalService) and the `approval/request` waterfall routes owned sessions' one-shot decisions to the client as `session/requestPermission`, mapping `allow-once`/`reject-once`/`cancelled` into the closed outcome vocabulary. The `acp` runtime profile ships as the `dsh-acp` bundle (`approval` + `acpRuntime` rows over console stdio, exiting on stdin EOF; the route follows `DEEPSEEK_API_KEY` like the headless/web rows). The session store's `Remove` is exercised by the resume flow exactly as documented. Documented reductions: MCP mounts are refused until the port has an MCP client seam, inline image prompts await the attachment admission seam, the model option advertises the session's fixed route only (the loop reads `AgentOptions` at creation; no catalogs or reasoning efforts), usage updates await the port's token meter, the persisted header carries no origin/parent/cwd so the resume checks are existence-only and the list's workspace filter is vacuous, and prompt cancellation flows through `session/cancel` only (the ported transport has no server-side request abort). — 45 console suites total (13 ACP).
- The Python SDK retires: `python/` is deleted (the .NET client SDK replaces it), along with its release pipelines (`.github/workflows/build-exe-for-python-sdk.yml`, `.gitlab-ci.yml`), its builder and smoke scripts (`scripts/build-exe-for-python-sdk.ts`, `build-python-release.py`, `smoke-python-runtime.py`, `check-macos-deployment-target.py`), the runtime-closure gate (`scripts/verify-runtime-closure.ts` + spec, its package.json script and run-gates entries), the pytest config, the uv dependabot entry, the python jobs in the PR workflow, the python workspace and lockfile entries, and the python SDK user guide. Every active reference is updated: the notices generator drops the Python closure and fetched-tool sections, the workspace/translation/config-source gates drop the python paths and tests, the SDK group READMEs (en+zh) name the retired Python SDK and its .NET replacement, and AGENTS.md's layout and testing rules drop the python surface. The code-runtime's CPython language backend is unrelated and stays.
- The TypeScript gate run validates the branch after the retirement (2026-09-01): `typecheck`, `lint`, `build`, `hygiene` (14/14), and all 32 `doc-sync` leaves pass, and the full unit suite is green except for host-environment failures on this machine (no PowerShell 7 on PATH, which the windows-acl runner and pwsh-sandbox suites need) and load-sensitive timeouts that pass standalone. The run surfaced branch-wide documentation violations in port notes that were never gated: 23 notes were reflowed (BOM stripped, `## Consequence` → `## Consequences`, one physical line per paragraph), 24 unpaired docs gained en/zh switchers and zh counterparts with `.i18n.yaml` records (15 phase-5 + 6 phase-6 notes, the base64url testing note, plan/README), 5 historical note pairs' dangling links to deleted Python files became plain text, `docs/config-catalog.md` and `THIRD_PARTY_NOTICES.md` were regenerated, Python SDK mentions left the user guide, testing docs, BENCHMARK.md, and the website manifest, the retired Python asset-build scripts were deleted, and `scripts/run-gates.spec.ts` dropped the hygiene-aggregate expectation for the removed runtime-closure gate.

- Phase 7 (parity, cutover, retire TS) starts (2026-09-01): the recorded-session replay harness lands. `src/Dsh/Dsh.Llm.Replay` ports `@deepseek-ai/dsh-llm-replay` — the script derivation splits recorded `assistant/chunk` events at every finish (packed `reasoning-chunks`/`tool-call-chunks` rows expand back into delta events; compaction summaries marked as LLM stream calls become canonical streams), the `replay.override.json` sidecar (whole-script or `{ patches }` augmentation with strict chunk validation), per-session binding by first-call order with the end-of-run consumption check, `{{session:N}}`/`{{fromRequest:}}` placeholder resolution, and env-driven config (`DSH_SNAPSHOT_FILE`/`OVERRIDE`/`CHILD_FILES`/`PROVIDER`/`MODEL`). The CLI gains the `replay` spine row and the snapshot overlay patch (disables `deepseek`/`mock`, mounts `replay`), and the headless/acp runtime rows follow `DSH_SNAPSHOT_PROVIDER/MODEL`. The headless ordering bug is fixed: the turn now starts only after the loader settles (`appReady.Commit`), so later rows mount before the first model call. `tests/Dsh.Snapshot.Tests` (5 console suites green) covers derivation, packed expansion, override validation, the consumption check, and an e2e where the real dsh CLI subprocess replays the fs-read fixture end to end (recorded usage 2882/75 and reasoning text persist in the log). Measured drift for the next rounds: the session log envelope serializes `$type`/PascalCase/flat instead of `{type, seq, time, data}` camelCase, the header lacks `cwd`/`delegationDepth`, missing events (`permission/preset`, `sandbox/mode`, `approval/policy`, `agent/inbox/spliced`, `session/title`), tool-name drift (`read`/`write`/`edit` vs `fs_read`/`fs_write`), the error reason serializes `failure` instead of `error`, and empty optionals must be omitted.
- The session-log storage envelope aligns with the TS wire format (2026-09-01): `SessionLogFormat` writes one event per line as `{"type","seq","time","data",…}` with camelCase payloads, canonical empty optionals absent, provenance (`sourceEventSeqs`) range-encoded (`[start, end]` pairs for runs of three or more), and the message-surface fields (`surfaceOp`) at the record level — the committed snapshot fixtures' exact spelling, so the .NET log is byte-comparable after normalization. The reader parses the same shape (including projected fixtures without seq/time) and restores the envelope (Id = `evt-{seq}`) for the resume path. The session header gains `cwd`, `delegationDepth`, `parentSession`, and `seedLength` (TS key order); `AgentOptions` carries the new fields and the ACP server passes the client's `session/new` cwd through. Payload shapes were pinned to the fixture spellings: message keys order `content, source, role, id`, `kind`-discriminated sources, `error` (not `failure`) on the error turn reason with `code, message` order, `interrupted`/`startsSeries` omitted when false. The pinned persistence fixture, the spike smoke pin, and the snapshot e2e were moved to the new format; persistence 16/16, spike 20/20, ACP 13/13, CLI 17/17, SDK server 8/8, SDK client 11/11, web host 113/113, snapshot 5/5.
- The session-start baseline and the durable inbox land (2026-09-01): the log now opens with the recorded policy baseline — `permission/preset`, `sandbox/mode`, `approval/policy` appended at session creation in the recorded order (the snapshot overlay mounts the `policyBaseline` row with the recorded values; the headless run creates its session only after the loader settles so the baseline listeners catch it). The inbox appends durable `agent/inbox/spliced` events per mutation (insert + consume splices around `turn/start`, `removedCount` absent when zero, `outcome: canceled` on clears). The runtime-context snapshot message lands: the `policyContext` row registers the sandbox/approval providers with the exact TS sentences, the projection renders the TS `joinContextSections` text (prefix + blank-line-joined sections) with `{name, text}` sections, and the loop joins with blank lines. The fallback title appends as a durable `session/title` (TS caps: 5 words/40 bytes) flushed after the runtime-context message to match the recorded fixture position. The fs tools rename to the TS surface (`read`/`write`) and render the absolute path (the fixtures' `{{cwd}}/…` spelling); the tool-result `meta` now carries the structured outcome. A stale static serializer-cache bug (the wire projections cached the polymorphic resolver before plugin event types registered, silently dropping `agent/inbox/spliced` frames) is fixed with a registry revision: the wire projections rebuild on registration. fs-read now replays to a completed turn with a log whose event sequence and payloads match the fixture (baseline, splices, context message, title at the recorded seqs); remaining drift is the normalizer surface (packed rows, {{system}}/{{tools}}/{{cwd}}/{{message:N}} tokens, seq/time projection).
- The session-corpus normalizer and runner land (2026-09-01): `SnapshotNormalizer` ports the TS normalize/identity modules (relationship-preserving `{{session:N}}`/`{{message:N}}` redaction, clock zeroing, provenance decoding, request-header `{{system}}`/`{{tools}}` tokenization, persistence-envelope omission, and the canonical packed-row repack), and the corpus runner replays every `snapshots/session/*` scenario through the real dsh CLI with per-scenario PASS/DRIFT/SKIP reporting. Systematic parity fixes this round: the headless stdout/stderr projection (reasoning blocks to stderr, final text + `\n`, exit 0 only on completed turns), message ids minted as dash-form UUIDs (the redactor's recognized shape), per-message-type key orders (user content/source/role/id, assistant role/content/source/id, tool source/content/role/id) and the finish-chunk failure `{message, code}` order via the storage writer, `DSH_PERMISSION_MODE` driving the policy baseline (workspace-write/read-only presets derive ask approval), and the model fallback for prompt-block scenarios. First corpus run: 5 passed, 57 drifted, 9 skipped (posix/pwsh), 2 errored of 73; the drift matrix is now precise: 25 scenarios blocked on unported tools (subagent 7, read_image 4, run_code 3, edit 2, cordis_define 2, cordis_inspect_query, lsp, ralph, skill, subagent_acp, subagent_codex, terminal_signal), and the rest are tool-render/adapter-detail parity (bash result text, request/context contextWindow, subagent children session counts).
- Tool-render parity lands (2026-09-01): the write tool's durable `{diffs}` meta now derives from a jsdiff-faithful hunk diff — `HunkDiffs` ports jsdiff 9's line tokenizer (terminator-attached tokens), the Myers edit script (with the leading common run made explicit), and `structuredPatch` hunk assembly (3 context lines, merge at at most 2 x context), verified byte-equal against the real package on 300 randomized cases plus pinned reference cases in Dsh.Fs.Tests (27/27). The provider captures the overwrite diff basis (`before`) with the TS bound semantics (`diffBasisMaxBytes`, default 10 MiB; null when the new content or the existing file is at/above the bound, binary, or invalid UTF-8), LF-normalized like the TS `readTextForDiff`; the scenario's fs-sandbox bound (64 bytes) reaches the CLI through `DSH_FS_DIFF_BASIS_MAX_BYTES`. Tool-result errors now preserve the TS name/code (`IToolErrorInfo` on `FsError` -> `{name: "FsError", code: "FS_NOT_FOUND"}`) instead of `{Error, UNKNOWN}`. bash/todo stop persisting raw result meta (`PersistMeta: false`), and the replay shell route runs Git-bash `sh` on Windows (`DSH_SHELL_PATH`) so recorded POSIX commands reproduce. Exact-model adapter defaults land: the replay provider exposes per-model capability metadata (contextWindow/defaultMaxTokens/defaultReasoningEffort/reasoningEfforts from the scenario's llm-replay block via `DSH_SNAPSHOT_MODEL_META`), the loop resolves the request config against it (TS `resolveCallWithInfo` semantics), the header records the resolved config with `adapterDefaults` flags (config key order provider/model/maxTokens/reasoningEffort), and `request/context` carries `contextWindow`; the policy baseline now derives mode/approval from the preset (workspace-write/read-only -> ask) so `permission:` scenarios match, and the normalizer scrubs the JSON-escaped cwd spelling in model-visible policy text. Corpus: 17 passed, 47 drifted, 9 skipped (posix/pwsh), 0 errored of 73 — up from 5/57/9/2; new passes include fs-write-overwrite (hunk diffs), fs-delete-recreate (FsError identity), fs-write-overwrite-bounded (diff basis + model metadata), session-sandbox-root (preset derivation). The 47 remaining drifts are fully categorized: 25 on unported tools (subagent 7, read_image 4, run_code 3, edit 2, cordis_define 2, cordis_inspect_query, lsp, ralph, skill, subagent_acp, subagent_codex, terminal_signal), hook/invoked events at tool points (10 hook-cc/codex scenarios), and single items: compaction overflow recovery, DeepSeek web-search provider + `web/deepseek-search-llm-request`, web-fetch fixture server, bash-spill result text, parallel-tool-calls ordering, repeat-tool-reminder event, llm/retry (empty-response-retry), workflow run events. Full sweep: 46/46 console test projects green.
- Tool-result spill, parallel dispatch, repeat reminders, and the edit tool land (2026-09-01): the result-retention policy ports to the tool pipeline (`SpillPolicy` + the scheduler's post-execute arm): a plain-text result larger than the inline cap spills to the session spill store and the model-facing text becomes a bounded head/tail preview plus the `(Omitted N bytes. Full formatted result stored at: <locator>. Use read with offset/limit, or grep this path to search within it.)` notice, with the notice's byte cost reserved inside the cap; the fs-composition scenarios' 800-byte cap reaches the CLI via `DSH_SNAPSHOT_SPILL_MAX_BYTES` (extracted from the scenario's or its composition owner's cordis.snapshot.yml), and `DSH_SNAPSHOT_SPILL_ROOT` mirrors the TS `snapshotSpillRoot` hash so both fixture and run locators normalize identically. The local spill provider keeps POSIX-style roots verbatim (a Windows drive prefix would shift every notice's numbers), and the normalizer tokenizes snapshot/cwd-rooted spill paths as `{{spillLocator:<name>}}`. The tool scheduler now logs all `tool/call` events up front (the TS dispatch pass) before executing and appending results in model order — the recorded parallel-dispatch interleaving. The repeat-tool-reminder guard counts on committed `tool/result` events (the TS post-execute timing), stamps the reminder source with the `tool × count` summary, and delivers through the agent's next-step inbox (the recorded `agent/inbox/spliced` insert + consume pair) instead of a direct append. The fs edit tool ports with the observation policy: reads and mutations record per-session presence/absence, writes derive replaceIfVersion/createIfAbsent intents from that state, edits refuse unobserved targets with the remediated `edit requires reading ... first — read the file, then retry` (FS_NOT_OBSERVED) and confirmed-absent targets with not-found, and the provider's atomic literal edit (unique-match by default, replace_all, FS_AMBIGUOUS_EDIT/FS_EDIT_NOT_FOUND/FS_STALE_VERSION) feeds the hunk-diff meta. Corpus: 22 passed, 42 drifted, 9 skipped (posix/pwsh), 0 errored of 73 — up from 17/47/9/0; new passes: bash-spill (spill notice), parallel-tool-calls (dispatch order), repeat-tool-reminder (inbox reminder), fs-edit and fs-policy-reject (edit + observation policy). Dsh.Fs.Tests grew to 32 (edit/policy/literal-edit/hunk-diff parity). Remaining drift stays categorized: 24 unported tools, 10 hook/invoked scenarios, compaction overflow recovery, DeepSeek web-search provider + llm-request event, web-fetch fixture server, session-query read tool, llm/retry policy (empty-response-retry), workflow run events.
- Hooks and subagents land (2026-09-01): the headless profile mounts the claude-code and codex hook bridges (`hooksClaudeCode`/`hooksCodex` rows with `DSH_HOOKS_CC_CONFIG`/`DSH_HOOKS_CODEX_CONFIG` env channels; the runner points them at the scenario's workspace hooks.json/codex-hooks.json derived from the fixture dialect), and all 14 hook scenarios plus packed-chunks pass: the hook/invoked key order pins handlerId before matcher, a post-execute block records no error identity (ToolResultEvent.Error null when the failure has neither name nor code), post-tool contexts deliver AFTER the durable tool/result (deferred via a session/event handoff so the recorded result-then-splice order reproduces), a hook ask records the durable approval/asked + approval/decided(rejected) pair and refuses with "the user rejected tool ...", the prompt-submit context joins before the fallback title (the title flush moved from the runtime-context message to the agent/request waterfall — the deterministic post-batch position), and the headless stdout concatenates ALL assistant text blocks (the Stop-hook continuation's earlier reply now appears). The subagent tool ports with a real child loop: the in-process runner spawns a child session (header origin "subagent", parentSession, delegationDepth+1) through the agent loop, sends the task, awaits idle, and maps the child's turn reason to the delegation stop reason (max-tokens/error/aborted/blocked with the partial text and the recorded failure diagnostic), refuses delegations beyond the depth ceiling with "subagent depth N exceeds maxDepth 2", and surfaces the child's final text as the tool result (no persisted meta); the base bundle mounts the `subagentTool` row (provider "subagent") and the ask-user rows, the child ask refuses with the DELEGATED_CALLER guidance, the corpus runner passes the recorded child logs via `DSH_SNAPSHOT_CHILD_FILES` and counts persisted sessions, and the harvest prefers the top-level session. Corpus: 42 passed, 22 drifted, 9 skipped (posix/pwsh), 0 errored of 73 — up from 22/42/9/0; new passes: the 14 hook scenarios + packed-chunks, subagent-spawn-in-process/multi/parallel/max-tokens-partial/child-question-rejection/depth-two-rejection. Remaining drift: unported tools (read_image 4, run_code 3, cordis_define 2, cordis_inspect_query, lsp, ralph, skill, subagent_acp, subagent_codex, terminal_signal), subagent-published-run-failure (a scripted TS-internal publish failure), compaction overflow recovery, llm/retry (empty-response-retry), DeepSeek web-search provider, web-fetch fixture server, workflow run events, pty sandbox backend.